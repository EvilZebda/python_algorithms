import sys

# Урок 2. Задание 3. Сформировать из введенного числа обратное по порядку входящих в него цифр и вывести на экран.
# Например, если введено число 3486, то надо вывести число 6843

number = input("Введите число: ")
print(f'Обратное число: {number[::-1]}')

print(f'Под переменную было выделено памяти: {sys.getsizeof(number)}')

''' Примечание: затраты памяти напрямую зависят от того, какой длинны число будет введено
При вводе 9 результат будет 50
При вводе 123456789 результат будет 58 '''
